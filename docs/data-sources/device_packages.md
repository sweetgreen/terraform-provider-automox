---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "automox_device_packages Data Source - automox"
subcategory: ""
description: |-
  Software packages and available patches for one device.
  This is per-device by necessity — Automox exposes no fleet-wide package listing — so reading it across many devices means one request each. Prefer automox_policy_stats for fleet compliance.
---

# automox_device_packages (Data Source)

Software packages and available patches for one device.

This is per-device by necessity — Automox exposes no fleet-wide package listing — so reading it across many devices means one request each. Prefer `automox_policy_stats` for fleet compliance.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `device_id` (Number) The device whose packages to read.

### Optional

- `installed` (Boolean) Filter to installed packages (`true`) or to outstanding ones (`false`). Omit for both.

### Read-Only

- `packages` (Attributes List) (see [below for nested schema](#nestedatt--packages))

<a id="nestedatt--packages"></a>
### Nested Schema for `packages`

Read-Only:

- `create_time` (String)
- `cve_score` (Number) Highest CVSS score among this package's CVEs, when scored.
- `cves` (List of String)
- `display_name` (String)
- `group_ignored` (Boolean) Ignored for the device's whole server group.
- `id` (Number)
- `ignored` (Boolean) Ignored on this device specifically.
- `installed` (Boolean)
- `is_managed` (Boolean)
- `name` (String)
- `repo` (String) Where the package comes from, such as `Apple`.
- `requires_reboot` (Boolean)
- `severity` (String) Automox severity, such as `critical` or `no_known_cves`.
- `version` (String)
